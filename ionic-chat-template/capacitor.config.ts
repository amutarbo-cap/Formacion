import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.chat',
  appName: 'ionic-chat-template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
